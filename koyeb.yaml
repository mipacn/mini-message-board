# .koyeb.yaml

version: 2
services:
  - name: mini-message-board
    type: web
    # The 'run' section defines the command to start your application
    run:
      commands:
        - npm start # This is your run command
    ports:
      - port: 8000 # Your app should listen on this port
        protocol: http
